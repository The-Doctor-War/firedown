<?xml version="1.0" encoding="utf-8"?>
<!-- One quality variant row. Mirrors the captions row rhythm (left control +
     stacked title/meta) so the two picker sections read as one. Title = the
     resolution description; meta = "<stream type> · <codec>". The loud filled
     "video+audio" chip that used to sit here was dropped — it repeated the
     same words on every row and fought the radio for attention. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/file_variants_item"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="?attr/selectableItemBackground"
    android:paddingHorizontal="16dp"
    android:paddingVertical="11dp">

    <com.google.android.material.radiobutton.MaterialRadioButton
        android:id="@+id/radio_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clickable="false"
        android:focusable="false"
        android:minWidth="0dp"
        android:minHeight="0dp"
        app:useMaterialThemeColors="true" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="12dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/stream_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:textColor="?attr/colorOnSurface"
            android:textStyle="bold"
            android:ellipsize="end"
            android:maxLines="1"
            tools:text="1080p (1920 x 1080)" />

        <!-- Single meta line: "<stream type> · <codec>". Either part may be
             empty; the adapter joins with " · " only when both are present. -->
        <TextView
            android:id="@+id/stream_info"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textSize="12sp"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:ellipsize="end"
            android:maxLines="1"
            tools:text="video + audio · av01 + mp4a" />

    </LinearLayout>

</LinearLayout>
